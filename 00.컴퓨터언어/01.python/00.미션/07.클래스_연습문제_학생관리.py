''' 
문제:
학생을 나타내는 클래스를 만들어야 합니다. 이 클래스는 다음과 같은 요구사항을 충족해야 합니다.
Student 클래스를 정의하세요.
각 학생의 이름, 학번, 과목별 성적 정보를 저장할 수 있어야 합니다.
1. 학생의 이름과 학번을 설정하는 set_info(name, student_id) 메서드를 클래스에 추가하세요.
2. 과목 이름과 해당 과목의 성적을 추가할 수 있는 add_grade(subject, grade) 메서드를 클래스에 추가하세요.
3. 학생의 모든 과목 성적을 조회하는 get_grades() 메서드를 클래스에 추가하세요.
이 클래스를 사용하여 학생 객체를 생성하고 학생의 이름, 학번, 과목별 성적을 설정하고 조회할 수 있어야 합니다.
이 문제를 해결하기 위한 파이썬 클래스를 만들고, 주어진 요구사항에 따라 메서드를 구현해보세요. 필요하다면 추가적인 도움이 필요하거나 코드 예제가 필요하다면 알려주세요.'''

class Student:
    students = []
    student_ids = []
    grades = {}
    subjects = []
    
    def __init__(self):
        self.student = student

    def set_info(name):
        self.name = name
    
        
        students = students.append(self.name)
        student_ids = students_ids.append(self.student_id)
        return students,student_ids

    def add_grade(subject, grade):
            self.subject = subject
            self.grade = grade
            
            grades = {self.subject:self.grade}
            
    def get_grades(student_id):
            




